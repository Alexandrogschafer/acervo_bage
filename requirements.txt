# Dependências do ACERVO_BAGE.
#
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#   pip install -r requirements.lock.txt   # para reproduzir o ambiente exato
#
# pyogrio não está listado por ser dependência direta do geopandas >= 1.0
# (é o motor de I/O vetorial); vem junto e aparece no lock.

geopandas>=1.0
rasterio>=1.3
pyproj>=3.6
requests>=2.31
pandas>=2.0
PyYAML>=6.0
shapely>=2.0

# Leitores das planilhas oficiais do IBGE (XLS BIFF8 de 2000/2010, XLSX, ODS),
# usados pelo reconhecimento do Censo (estudos/A03_expansao_adensamento/scripts/).
xlrd>=2.0
openpyxl>=3.1
odfpy>=1.4
